/** Shared HTTP path + DTO for model capability lookups. */
/** Batch capability probe (GET `?m=provider/model`). */
export const CAPABILITIES_PATH = '/api/chat-enhance.capabilities';
//# sourceMappingURL=capabilities-contract.js.map